import './globals.css';

export const metadata = {
  title: 'Scentra | Business Command Center',
  description: 'Perfume shop management dashboard'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
